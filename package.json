{
  "name": "self-introduction-station02",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:server": "swc src/server -d dist/server",
    "copy:client": "mkdir -p client-build && cp -r ../path/to/station1-react-app/dist/* ./client-build/",
    "build": "npm run build:server && npm run copy:client",
    "start": "node dist/server/index.js",
    "dev:server": "swc src/server -d dist/server --watch",
    "dev": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.9"
  },
  "devDependencies": {
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.11.21",
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  }
}
